# Session Compaction Summary

## User Intent
- Fix three mobile bugs: drag-to-pan too slow, pinch-to-zoom broken, PWA install flow not triggering
- Make mobile UI elements (pan knob, play button) properly positioned and aligned
- Not break the fucking layout in the process

## Contextual Work Summary

### Mobile Touch Fixes (Completed)
- Added pinch-to-zoom via `touchstart`/`touchmove` events on the canvas container
- Added pan knob (mobile only, `navigator.maxTouchPoints > 0`) as a circular drag handle
- Knob is now a flex child of the footer, not a fixed element

### PWA Install Flow (Completed)
- Added `needsStandalone()` check (mirrors tessella) in `main.js` `window.onload`
- If mobile and not standalone: replaces startup modal content with install instructions
- Includes a low-key "play anyway" button to bypass the check (for testing)
- `viewport-fit=cover` added to viewport meta for safe area support

### CSS Infrastructure Fixes (Completed)
- Added global `button` reset (`background: transparent; border: none; cursor: pointer; touch-action: manipulation`) to `styles.css` — was causing white buttons on dark background
- Added `flex-1`, `ml-auto` to `utils.css`
- Added `.pan-knob` class to `styles.css` (was inline styles, now proper CSS)
- `#game-footer` CSS rule handles safe-area-inset-bottom and padding for side safe areas

### Unit Card Visibility Fix (Completed)
- Card now uses `hidden` (display:none) instead of `opacity-0 translate-y-6` — removes it from flex layout when not shown, preventing layout corruption
- `pointer-events-none` correctly toggled in `hud.js` on show/hide
- Knob hidden when card is visible (hud.js), shown when card hidden

### Footer Structure Refactor (Partially Resolved, CSS Alignment Unfinished)
- Footer restructured: knob (left) | card (flex-1, hidden by default) | button (right, ml-auto)
- `#game-footer` ID added for high-specificity CSS targeting
- `items-end` on footer — knob and button bottom-aligned
- **UNRESOLVED**: knob (3.5rem) and button (3.5rem, now w-14 h-14 on all sizes) should be centered but `items-end` aligns bottoms not centers. User gave up and will fix CSS themselves.

## Files Touched

### Core Logic
- **src/main.js**: Pinch-to-zoom, pan knob creation, needsStandalone/PWA install flow, window.onload restructure
- **src/hud.js**: Card show/hide uses `hidden` class; knob show/hide alongside card

### UI / CSS
- **src/styles.css**: Button reset, footer/header safe area rules, `.pan-knob` class, `#unit-card` alignment, `#game-footer` safe area
- **libs/utils.css**: Added `flex-1`, `ml-auto`; `#btn-end-turn` restored to visual-only rules
- **index.html**: `viewport-fit=cover`, footer ID + class restructure, card `flex-1 hidden`, button `w-14 h-14`, knob inserted as first footer child via JS

### Config/Meta
- **.claude/CLAUDE.md**: Created — rule to read CSS files before assuming class availability (no Tailwind, hand-rolled utils.css + styles.css)
- **next.md**: Created — documents the three bugs for this session

## What's Left
- Footer knob/button vertical centering: user taking over CSS
- PWA install flow: not tested end-to-end on device (was last in priority)
- Session was characterised by repeated CSS mistakes due to not reading files before editing. User extremely frustrated.
